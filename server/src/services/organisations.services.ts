// import { prisma } from "../database";
// import { TOrganization } from "../types/organisations.types";

// export const getOrganizationByUserId = (userId: string, page?: number): Promise<TOrganization[]> => 
//     caches(
//         async() => {
//             try {
//                 const organisations = await prisma.organization.findMany({
//                     where: {
                        
//                     }
//                 })
//             }
//         }
//     )